#include<stdio.h>

void main(){
    float num;
    printf("Enter the floating point number: ");
    scanf("%f", &num);
    printf("%d", (int)num);
    int whole_integer = (int)num ;

    // printf("The right most integer value is %d", rightmost_value);
}